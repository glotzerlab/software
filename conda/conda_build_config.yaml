# From conda-forge-pinning
zip_keys:
  -
    - python
    - numpy

python:
  # part of a zip_keys: python, numpy
  - 3.14.*=*_cp314

numpy:
  # part of a zip_keys: python, numpy
  - 2.4.2

pybind11_abi:
  - 4
