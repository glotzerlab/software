

RUN apt-get update && apt-get install -y --no-install-recommends \
  dkms \
  infiniband-diags \
  ibacm \
  mstflint \
  libibmad-dev \
  libibumad-dev \
  opensm \
  srptools \
  ibutils \
  ibverbs-utils \
  rdmacm-utils \
  perftest \
  numactl \
  libnuma-dev \
  libnl-3-200 \
  libnl-route-3-200 \
  libnl-route-3-dev \
  libnl-utils \
  && rm -rf /var/lib/apt/lists/*

# use ubuntu provided ibverbs
RUN apt-get update && apt-get install -y --no-install-recommends \
  libibverbs-dev \
  ibverbs-providers \
  librdmacm-dev \
  && rm -rf /var/lib/apt/lists/*
